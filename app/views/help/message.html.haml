- title t(:'help.header')

- if params[:id].to_s.end_with? '_markdown'
  = raw(Maruku.new(t params[:id]).to_html)
- else
  %p= t params[:id]

%p= link_to t(:'help.close_button'), ':back', 'data-role' => 'button', 'data-rel' => 'back', 'data-theme' => 'b'

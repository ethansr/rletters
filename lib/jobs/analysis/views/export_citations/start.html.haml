%li
  Export dataset as citations
  %ul
    - Document.serializers.each do |k, v|
      %li= link_to("Export as #{v[:name]}", :controller => 'datasets', :action => 'job_start', :id => dataset.to_param, :job_name => 'start_ExportCitations', :job_params => { :format => k })
